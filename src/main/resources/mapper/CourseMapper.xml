<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.PTever.mapper.CourseMapper">
    <select id="selectById" resultType="courseVO">
        SELECT C.COURSE_TITLE, C.COURSE_DESCRIPTION, C.COURSE_PRICE, C.COURSE_DISCOUNT, CR.REVIEW_CONTENT, CR.USER_ID
        FROM TBL_COURSE C
        LEFT OUTER JOIN TBL_COURSE_REVIEW CR
        ON C.COURSE_ID = CR.COURSE_ID
        WHERE COURSE_ID = #{id}
    </select>
</mapper>